class Feature2Screen1View extends StatelessWidget {
  const Feature2Screen1View({super.key});

  @override
  Widget build(BuildContext context) {
    return BlocBuilder<Feature2Screen1Bloc>(
      builder: (_, __) {
        return Container();
      },
    );
  }
}
